# Devlog — P2~P4 완성 + P5a·P10a (2026-07-04)

> 자율 진행 세션. 각 슬라이스를 feature 브랜치 → 실 검증 → dev 머지. 푸시 미승인.

## 완료 (dev=`77aef09`)

### P2 — 인프라 마감
- **잡큐**(`86f6925`): Postgres 잡 테이블 + `FOR UPDATE SKIP LOCKED` 원자적 claim + 지수백오프.
  생성 detach(POST /decks/generate) + 재접속 재생(GET /jobs/:id/stream) + 크래시 복구(오래 잠긴 running 재클레임).
  실 claude E2E: 서버 죽였다 재시작 → 12 이벤트 전체 재생 + 덱 영속.
- **Docker**(`5996f55`): app/web Dockerfile + nginx 프록시(SSE) + `docker compose up`. 실기동·서빙 검증.
- **설정 카탈로그**(`e87f31d`): 타입드 카탈로그(APP_SETTINGS_CATALOG) + DB KV 영속 + 부팅 rehydrate + 핫리로드.
  `ensureSchema` advisory lock으로 동시 부팅 DDL 레이스 방지.

### P3 — 딥리서치 / 할루시네이션 제로
- **데이터 파이프라인**(`d1c427b`): `packages/research`(leaf, LLM 콜백 주입) — 검색 어댑터(Tavily/Serper),
  URL fetch, extractFacts(소스ID 부착·지어낸 출처 폐기), buildCitations. 소스→팩트→인용 전파
  (outline factIds → slide citationIds → deck citations). 실 claude: 유저 텍스트 → 5팩트 → 역추적 무결성.
- **출처 슬라이드**(`1a2a67d`): references 레이아웃(hidden) 자동 추가 — 렌더러/익스포터 자동 렌더.

### P4 — HITL 게이트
- **계획 게이트**(`e150d75`): `generatePlans`(슬라이드별 designIntent/contentSummary),
  POST /decks/outline·/decks/plans 미리보기, 승인 outline+research 재사용. **시장 공백 차별화**.
  QA 봉합: slide_system 메타문구 누출 → 프롬프트 예시 + closing `.describe()`.
- **게이트 위저드 UI**(`b5c6ce2`): 프리셋별 오케스트레이션 + OutlineGate + PlanGate + 소스 입력 + Playwright.

### P5a — WYSIWYG
- **인라인 편집**(`70f9677`): 캔버스 텍스트/리스트 contentEditable + 저장(PATCH /decks/:id).
  QA 봉합: useWidth 콜백 ref(직접 URL/새로고침 캔버스 미렌더 버그).

### P10a — 접근성
- **검사기**(`77aef09`): WCAG 대비율 AA + alt 점검. GET /decks/:id/accessibility.

## QA로 잡은 실버그 5종
1. slide_system 메타 문구 누출 → 프롬프트 예시 + 레이아웃 `.describe()`
2. `ensureSchema` 동시 부팅 DDL 레이스 → advisory lock
3. PG 잡 테스트 workspace 전역 claim 간섭 → 테스트별 고유 workspace
4. 에디터 useWidth 마운트 시 ref null → 콜백 ref (Playwright가 적발)
5. 루트 vitest가 Playwright spec 수집 → vitest.config exclude

## 검증
단위 215 + 실 PG 통합 + 실 claude E2E 다수 + Playwright 3 + 풀파이프라인 헬스체크(생성→PPTX 유효).

## 남은 것
P5 드래그/리사이즈/undo · P6 갤러리/브랜드킷/PPTX추출 · P7 입력확장 · P8 이미지/차트 ·
P9 발표/PNG/화면비스윕(레이아웃 좌표 아키텍처 필요) · P10 번역/요약/MCP.
